# Environment Variables Template for Social Investment Club Backend

# --- Database Configuration ---
# Used by the application in development/production and by Alembic migrations
DATABASE_URL=postgresql+psycopg2://user:password@host:port/dbname

# Used by the application (FastAPI with async SQLAlchemy) in development/production
ASYNC_DATABASE_URL=postgresql+asyncpg://user:password@host:port/dbname

# Used by pytest for integration tests (sync operations like migrations)
SYNC_TEST_DB=postgresql+psycopg2://test_user:test_password@test_host:port/test_dbname

# Used by pytest for integration tests (async application operations)
ASYNC_TEST_DB=postgresql+asyncpg://test_user:test_password@test_host:port/test_dbname

# --- Authentication (Auth0) ---
AUTH0_DOMAIN=your_auth0_domain.auth0.com
AUTH0_AUDIENCE=your_auth0_api_audience_identifier
AUTH0_WHITELIST_ORGANIZATION_ID=your_auth0_organization_id

# --- External APIs ---
# Used by accounting_service to fetch market data
ALPHA_VANTAGE_API_KEY=your_alpha_vantage_api_key

# --- Application Settings ---
# Optional: Secret key for FastAPI application (e.g., for signing cookies if used later)
# Generate a strong random key, e.g., using: openssl rand -hex 32
# FASTAPI_SECRET_KEY=your_strong_random_secret_key

# --- Testing Flag ---
# This variable is typically set programmatically by tests (e.g., in conftest.py)
# It controls which database URL is used (production vs. test).
# You generally DON'T need to set this in your actual .env file for running the app.
# TESTING=false

